# Server-only Vercel Function configuration.
# Never prefix these names with PUBLIC_ or expose their values to Astro client code.
LEADS_WEBAPP_URL=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec
LEADS_WEBAPP_SECRET=replace-with-a-long-random-shared-secret
